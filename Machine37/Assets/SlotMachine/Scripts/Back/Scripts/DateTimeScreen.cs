using CG.MagicMenu;

namespace Com.Back
{
    /// <summary>后台常驻叠加屏(DateTime.prefab 根节点)。F12 进后台时与 Menus 同时显示，整后台期间一直常驻（不被整屏切换关掉）。</summary>
    public class DateTimeScreen : Menu<DateTimeScreen>
    {
    }
}
