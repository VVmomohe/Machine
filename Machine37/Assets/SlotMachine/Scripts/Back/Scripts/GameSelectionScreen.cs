using CG.MagicMenu;

namespace Com.Back
{
    /// <summary>游戏选择屏(GameSelection.prefab 根节点)。密码正确后由密码网关打开；
    /// 其 opt0→GameSeting、opt1→ChangePSW（见 SelectionView 硬编码导航）。</summary>
    public class GameSelectionScreen : Menu<GameSelectionScreen>
    {
    }
}
